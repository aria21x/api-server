[phases.setup]
nixPkgs = ["python312Full", "postgresql", "redis"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "uvicorn apps.api_server.main:app --host 0.0.0.0 --port $PORT"

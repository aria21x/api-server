[phases.setup]
nixPkgs = ["python312", "postgresql", "redis"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building alert-worker'"]

[start]
cmd = "python -m apps.alert_worker.main"
